package EsPag33;

import java.lang.Math;

public BandieraNazionale {

  publis static final int SIZE = 10;

  //Colors[] colorArr = new Colors[SIZE];

  char[] colorArr = new char[SIZE];

  for (int el = 0; el < SIZE; el++) {
    int n = (int) (Math.random() * 3);      // Genera numeri n:  0 <= n <= 3
  }

}
