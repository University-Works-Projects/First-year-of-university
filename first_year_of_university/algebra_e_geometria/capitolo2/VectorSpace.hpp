#include <iostream>

using namespace std;

class VectorSpace {
private:

protected:
    int n;
    int point[];
public:
    VectorSpace (int in_n) {
        this -> n = in_n;
        this -> point[in_n];
    }

    int* creatwPoint () {
        
    }

    bool closedSum () {
        int point1[this -> n];
        for (int pos = 0; pos < this -> n; pos++) point1
        this -> point;
    }

};