/*

    # Accedere alla lunghezza di una dimensione di array multidimensionali:
        int x[][][] = new int[a][b][c];
        System.out.println(x.length + " " + x[1].length + " " + x[0][1].length);
                            ^^^^^^^	         ^^^^^^^^	          ^^^^^^^^^^^
                               a 	             b		             c

    # Generazione di numeri casuali:
    var = 1 + Math.random() * (max); // Il range di var è: max >= var >= 1

    # Come prendere diversi tipi di input:
        UserInput.nextBoolean() 	boolean
        UserInput.nextByte() 		byte
        UserInput.nextDouble() 		double
        UserInput.nextFloat() 		float
        UserInput.nextInt() 		int
        UserInput.nextLine() 		String
        UserInput.nextLong() 		long
        UserInput.nextShort() 		short

    # La differenza tra public e static methods:
        - Static methods can be called without creating objects
        - Public methods must be called by creating objects

    # Classe astratta https://www.html.it/pag/51820/classi-astratte-in-java/

    # Metodi get e set:
        - get: Restituisce il valore di una variabile di istanza.
        - set: Assegna un valore ad una variabile di istanza.

    # equals method in classes

*/

public class OtherFeatures {
    public static void main (String[] args) {
        System.out.println("eskere");
    }
}
