/*

    Abstract viene utilizzata come una sorta di "comun denominatore" fra classi.

    Interface viene utilizzato per mostrare i metodi di una funzione (al fine di avere maggiore chiarezza).
    Si può interpretare l'interface di java come (in modo mooolto analogo) il mettere in "cima" ad un programma
    scritto in C, le funzioni che verranno successivamente scritte, oppure ad un file nomeFile.h in C.

    Un'interfaccia può essere comunque implementata in maniera diversa da classi diverse
    inoltre:
        - Se classi diverse implementano la stessa interfaccia, allora rispetto all'interfaccia gli oggetti sono dello stesso tipo    
        - Se una classe implementa più interfacce che definiscono lo stesso metodo (esattamente, stessa signature e stessi parametri) non c'è conflitto

*/